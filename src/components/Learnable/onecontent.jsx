import React from "react";

const Onecontent = ({ title, content }) => {
    return(
        <div>
            <h4>{title}</h4>
            <p>{content}</p>
        </div>
    );
}

export default Onecontent;