import React from "react";

const Apply = ({title, content}) => {
    return(
        <div className="Apply-content">
            <h3>{title}</h3>
            <p>{content}</p>
        </div>
    );
}


export default Apply;